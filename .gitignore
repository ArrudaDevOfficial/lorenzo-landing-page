node_modules
dist
.output
.vinxi
.tanstack
*.local
.env
.env.*
.DS_Store

# Gerado automaticamente pelo TanStack Router — não editar/versionar
src/routeTree.gen.ts

# Fotos originais em alta resolução (HEIC/JPG brutos) — as versões
# otimizadas usadas no site ficam em public/images.
iMG/
